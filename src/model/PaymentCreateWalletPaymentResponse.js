/**
 * Kenar API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PaymentCreateWalletPaymentResponse model module.
 * @module model/PaymentCreateWalletPaymentResponse
 * @version 0.1.0
 */
class PaymentCreateWalletPaymentResponse {
    /**
     * Constructs a new <code>PaymentCreateWalletPaymentResponse</code>.
     * @alias module:model/PaymentCreateWalletPaymentResponse
     */
    constructor() { 
        
        PaymentCreateWalletPaymentResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PaymentCreateWalletPaymentResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaymentCreateWalletPaymentResponse} obj Optional instance to populate.
     * @return {module:model/PaymentCreateWalletPaymentResponse} The populated <code>PaymentCreateWalletPaymentResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaymentCreateWalletPaymentResponse();

            if (data.hasOwnProperty('payment_url')) {
                obj['payment_url'] = ApiClient.convertToType(data['payment_url'], 'String');
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaymentCreateWalletPaymentResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaymentCreateWalletPaymentResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['payment_url'] && !(typeof data['payment_url'] === 'string' || data['payment_url'] instanceof String)) {
            throw new Error("Expected the field `payment_url` to be a primitive type in the JSON string but got " + data['payment_url']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }

        return true;
    }

/**
     * Returns The address you should redirect the user to
     * @return {String}
     */
    getPaymentUrl() {
        return this.payment_url;
    }

    /**
     * Sets The address you should redirect the user to
     * @param {String} paymentUrl The address you should redirect the user to
     */
    setPaymentUrl(paymentUrl) {
        this['payment_url'] = paymentUrl;
    }
/**
     * Returns Token of the transaction. used for next operations
     * @return {String}
     */
    getToken() {
        return this.token;
    }

    /**
     * Sets Token of the transaction. used for next operations
     * @param {String} token Token of the transaction. used for next operations
     */
    setToken(token) {
        this['token'] = token;
    }

}



/**
 * The address you should redirect the user to
 * @member {String} payment_url
 */
PaymentCreateWalletPaymentResponse.prototype['payment_url'] = undefined;

/**
 * Token of the transaction. used for next operations
 * @member {String} token
 */
PaymentCreateWalletPaymentResponse.prototype['token'] = undefined;






export default PaymentCreateWalletPaymentResponse;

