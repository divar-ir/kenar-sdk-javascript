/**
 * API کنار
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PostGeneralDataPostVideo model module.
 * @module model/PostGeneralDataPostVideo
 * @version 0.1.0
 */
class PostGeneralDataPostVideo {
    /**
     * Constructs a new <code>PostGeneralDataPostVideo</code>.
     * @alias module:model/PostGeneralDataPostVideo
     * @param duration {String} زمان ویدیو به ثانیه
     * @param name {String} نام ویدیو (از فیلد `video_name` در پاسخ آپلود)
     * @param thumbnailName {String} کاور ویدیو (از فیلد `thumbnail_name` در پاسخ آپلود). فریم اول ویدیوی آپلود‌شده است.
     */
    constructor(duration, name, thumbnailName) { 
        
        PostGeneralDataPostVideo.initialize(this, duration, name, thumbnailName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, duration, name, thumbnailName) { 
        obj['duration'] = duration;
        obj['name'] = name;
        obj['thumbnail_name'] = thumbnailName;
    }

    /**
     * Constructs a <code>PostGeneralDataPostVideo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PostGeneralDataPostVideo} obj Optional instance to populate.
     * @return {module:model/PostGeneralDataPostVideo} The populated <code>PostGeneralDataPostVideo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PostGeneralDataPostVideo();

            if (data.hasOwnProperty('duration')) {
                obj['duration'] = ApiClient.convertToType(data['duration'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('thumbnail_name')) {
                obj['thumbnail_name'] = ApiClient.convertToType(data['thumbnail_name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PostGeneralDataPostVideo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PostGeneralDataPostVideo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PostGeneralDataPostVideo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['duration'] && !(typeof data['duration'] === 'string' || data['duration'] instanceof String)) {
            throw new Error("Expected the field `duration` to be a primitive type in the JSON string but got " + data['duration']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['thumbnail_name'] && !(typeof data['thumbnail_name'] === 'string' || data['thumbnail_name'] instanceof String)) {
            throw new Error("Expected the field `thumbnail_name` to be a primitive type in the JSON string but got " + data['thumbnail_name']);
        }

        return true;
    }

/**
     * Returns زمان ویدیو به ثانیه
     * @return {String}
     */
    getDuration() {
        return this.duration;
    }

    /**
     * Sets زمان ویدیو به ثانیه
     * @param {String} duration زمان ویدیو به ثانیه
     */
    setDuration(duration) {
        this['duration'] = duration;
    }
/**
     * Returns نام ویدیو (از فیلد `video_name` در پاسخ آپلود)
     * @return {String}
     */
    getName() {
        return this.name;
    }

    /**
     * Sets نام ویدیو (از فیلد `video_name` در پاسخ آپلود)
     * @param {String} name نام ویدیو (از فیلد `video_name` در پاسخ آپلود)
     */
    setName(name) {
        this['name'] = name;
    }
/**
     * Returns کاور ویدیو (از فیلد `thumbnail_name` در پاسخ آپلود). فریم اول ویدیوی آپلود‌شده است.
     * @return {String}
     */
    getThumbnailName() {
        return this.thumbnail_name;
    }

    /**
     * Sets کاور ویدیو (از فیلد `thumbnail_name` در پاسخ آپلود). فریم اول ویدیوی آپلود‌شده است.
     * @param {String} thumbnailName کاور ویدیو (از فیلد `thumbnail_name` در پاسخ آپلود). فریم اول ویدیوی آپلود‌شده است.
     */
    setThumbnailName(thumbnailName) {
        this['thumbnail_name'] = thumbnailName;
    }

}

PostGeneralDataPostVideo.RequiredProperties = ["duration", "name", "thumbnail_name"];

/**
 * زمان ویدیو به ثانیه
 * @member {String} duration
 */
PostGeneralDataPostVideo.prototype['duration'] = undefined;

/**
 * نام ویدیو (از فیلد `video_name` در پاسخ آپلود)
 * @member {String} name
 */
PostGeneralDataPostVideo.prototype['name'] = undefined;

/**
 * کاور ویدیو (از فیلد `thumbnail_name` در پاسخ آپلود). فریم اول ویدیوی آپلود‌شده است.
 * @member {String} thumbnail_name
 */
PostGeneralDataPostVideo.prototype['thumbnail_name'] = undefined;






export default PostGeneralDataPostVideo;

